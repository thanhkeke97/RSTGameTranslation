<UserControl x:Class="RSTGameTranslation.FancyBalloon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="Auto" MinHeight="60" Width="280">
    <Border BorderBrush="#FF454545" BorderThickness="1" Background="#CC252525" CornerRadius="5">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="txtTitle" Text="Title" FontWeight="Bold" Foreground="White" FontSize="14"/>
            <TextBlock x:Name="txtMessage" Grid.Row="1" Text="Message" Foreground="#FFCCCCCC" TextWrapping="Wrap" Margin="0,5,0,0"/>
        </Grid>
    </Border>
</UserControl>