<Window x:Class="RSTGameTranslation.QuickstartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RSTGameTranslation"
        mc:Ignorable="d"
        Title="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Title]}"
        Height="600" Width="800"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Icon="/media/AppIcon.ico">
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="15" CaptionHeight="10" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Modern Button Style with Smooth Animations -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="borderScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" 
                                                           Storyboard.TargetProperty="ScaleX" 
                                                           To="1.03" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" 
                                                           Storyboard.TargetProperty="ScaleY" 
                                                           To="1.03" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" 
                                                           Storyboard.TargetProperty="ScaleX" 
                                                           To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" 
                                                           Storyboard.TargetProperty="ScaleY" 
                                                           To="1" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" 
                                                           Storyboard.TargetProperty="ScaleX" 
                                                           To="0.97" Duration="0:0:0.08"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" 
                                                           Storyboard.TargetProperty="ScaleY" 
                                                           To="0.97" Duration="0:0:0.08"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Neutral button used for subtle actions -->
        <Style x:Key="NeutralButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{DynamicResource NeutralBgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="neutralBorderScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource Surface2Brush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#22000000" BlurRadius="8" ShadowDepth="1" Opacity="0.15"/>
                                    </Setter.Value>
                                </Setter>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="neutralBorderScale" Storyboard.TargetProperty="ScaleX" To="1.03" Duration="0:0:0.12"/>
                                            <DoubleAnimation Storyboard.TargetName="neutralBorderScale" Storyboard.TargetProperty="ScaleY" To="1.03" Duration="0:0:0.12"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="neutralBorderScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.12"/>
                                            <DoubleAnimation Storyboard.TargetName="neutralBorderScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.12"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                <Setter TargetName="border" Property="Effect" Value="{x:Null}"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="neutralBorderScale" Storyboard.TargetProperty="ScaleX" To="0.97" Duration="0:0:0.06"/>
                                            <DoubleAnimation Storyboard.TargetName="neutralBorderScale" Storyboard.TargetProperty="ScaleY" To="0.97" Duration="0:0:0.06"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Accent (solid) button style with Animation -->
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#330066CC" BlurRadius="12" ShadowDepth="2" Opacity="0.45"/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="accentBorderScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.95"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="accentBorderScale" Storyboard.TargetProperty="ScaleX" To="1.04" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="accentBorderScale" Storyboard.TargetProperty="ScaleY" To="1.04" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="accentBorderScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="accentBorderScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="accentBorderScale" Storyboard.TargetProperty="ScaleX" To="0.96" Duration="0:0:0.08"/>
                                            <DoubleAnimation Storyboard.TargetName="accentBorderScale" Storyboard.TargetProperty="ScaleY" To="0.96" Duration="0:0:0.08"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Window Control Button Style -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Width" Value="14"/>
            <Setter Property="Height" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Ellipse x:Name="circle" Fill="{TemplateBinding Background}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="circle" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">
        <!-- Main Window Frame -->
        <Border Name="MainBorder" Background="{DynamicResource SurfaceBrush}" CornerRadius="12" 
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="0" Opacity="0.6" Color="Black"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Title Bar -->
                    <RowDefinition Height="*"/>    <!-- Content -->
                    <RowDefinition Height="Auto"/> <!-- Footer -->
                </Grid.RowDefinitions>

                <!-- ========== TITLE BAR ========== -->
                <Border Name="HeaderBorder" Grid.Row="0" Background="{DynamicResource Surface2Brush}" 
                        CornerRadius="12,12,0,0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1"
                        MouseLeftButtonDown="Header_MouseLeftButtonDown"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                    <Grid Height="40" Margin="12,0">
                        <!-- Left: Logo + Title -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <Border Width="24" Height="24" CornerRadius="5" Margin="0,0,10,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#00aaff" Offset="0"/>
                                        <GradientStop Color="#00cc88" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Image Source="/media/AppIcon.ico" Width="16" Height="16" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Quick Start Guide" FontSize="13" FontWeight="SemiBold" 
                                       Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Right: Window Controls -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <!-- Skip Button -->
                            <Button Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Btn_Skip]}" Style="{StaticResource NeutralButton}" Height="30" Margin="0,0,8,0"
                                    Click="SkipButton_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>

                            <!-- Window Control Buttons -->
                            <Button Style="{StaticResource WindowControlButton}" Background="#FFBD2E"
                                    Click="MinimizeButton_Click" WindowChrome.IsHitTestVisibleInChrome="True"
                                    ToolTip="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Tooltip_Minimize]}">
                                <TextBlock Text="−" FontSize="10" FontWeight="Bold" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,1"/>
                            </Button>
                            <Button Style="{StaticResource WindowControlButton}" Background="#27C93F"
                                    Click="MaximizeButton_Click" WindowChrome.IsHitTestVisibleInChrome="True"
                                    ToolTip="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Tooltip_Maximize]}">
                                <TextBlock Text="□" FontSize="9" FontWeight="Bold" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,1"/>
                            </Button>
                            <Button Style="{StaticResource WindowControlButton}" Background="#FF5F57"
                                    Click="CloseButton_Click" WindowChrome.IsHitTestVisibleInChrome="True"
                                    ToolTip="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Tooltip_Close]}">
                                <TextBlock Text="×" FontSize="12" FontWeight="Bold" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,1"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- ========== CONTENT AREA ========== -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Progress Indicator -->
                    <Grid Grid.Row="0" Margin="20,15,20,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Step indicators -->
                        <Border Grid.Column="1" x:Name="Step1Indicator" Width="20" Height="20" CornerRadius="10" Background="{DynamicResource NeutralBgBrush}" Margin="0,0,5,0">
                            <TextBlock Text="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                        </Border>
                        <Border Grid.Column="2" x:Name="Step2Indicator" Width="20" Height="20" CornerRadius="10" Background="{DynamicResource NeutralBgBrush}" Margin="0,0,5,0">
                            <TextBlock Text="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                        </Border>
                        <Border Grid.Column="3" x:Name="Step3Indicator" Width="20" CornerRadius="10" Background="{DynamicResource NeutralBgBrush}" Margin="5,2,5,0" Height="20" VerticalAlignment="Top">
                            <TextBlock Text="3" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,2,0,0"/>
                        </Border>
                        <Border Grid.Column="4" x:Name="Step4Indicator" Width="20" Height="20" CornerRadius="10" Background="{DynamicResource NeutralBgBrush}" Margin="5,0">
                            <TextBlock Text="4" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                        </Border>
                        <Border Grid.Column="5" x:Name="Step5Indicator" Width="20" Height="20" CornerRadius="10" Background="{DynamicResource NeutralBgBrush}" Margin="5,0,0,0">
                            <TextBlock Text="5" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                        </Border>
                    </Grid>

                    <Rectangle Grid.Row="0" Height="1" Fill="{DynamicResource BorderBrush}" VerticalAlignment="Bottom" Margin="20,0"/>

                    <!-- Content area -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <Grid Margin="20">
                            <!-- Welcome Panel -->
                            <StackPanel x:Name="WelcomePanel" Visibility="Visible">
                                <TextBlock TextWrapping="Wrap" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Title]}"/>

                                <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Desc]}"/>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,10"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_HowItWorks]}"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="1." FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,10,10"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}"
                                                       Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Step1]}"/>

                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="2." FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,10,10"/>
                                            <TextBlock Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}"
                                                       Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Step2]}"/>

                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="3." FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,10,10"/>
                                            <TextBlock Grid.Row="2" Grid.Column="1" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}"
                                                       Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Step3]}"/>

                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="4." FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,10,10"/>
                                            <TextBlock Grid.Row="3" Grid.Column="1" TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}"
                                                       Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Step4]}"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Configure]}"/>

                                <StackPanel Margin="20,0,0,0">
                                    <TextBlock TextWrapping="Wrap" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Configure1]}"/>
                                    <TextBlock TextWrapping="Wrap" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Configure2]}"/>
                                    <TextBlock TextWrapping="Wrap" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Welcome_Configure3]}"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Language Settings Panel -->
                            <StackPanel x:Name="LanguagePanel" Visibility="Collapsed">
                                <TextBlock TextWrapping="Wrap" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Language_Title]}"/>

                                <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Language_Desc]}"/>

                                <Grid Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Source Language -->
                                    <StackPanel Grid.Column="0" Grid.Row="0">
                                        <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Language_Source]}"/>
                                        <ComboBox x:Name="SourceLanguageComboBox" Width="200" HorizontalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- Target Language -->
                                    <StackPanel Grid.Column="2" Grid.Row="0">
                                        <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Language_Target]}"/>
                                        <ComboBox x:Name="TargetLanguageComboBox" Width="200" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,10"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Language_CommonPairs]}"/>
                                        <WrapPanel>
                                            <Button Content="Japanese → English" Style="{StaticResource NeutralButton}" Margin="5" Padding="8,3" Click="CommonPair_Click" Tag="ja,en"/>
                                            <Button Content="Japanese → Vietnamese" Style="{StaticResource NeutralButton}" Margin="5" Padding="8,3" Click="CommonPair_Click" Tag="ja,vi"/>
                                            <Button Content="Korean → English" Style="{StaticResource NeutralButton}" Margin="5" Padding="8,3" Click="CommonPair_Click" Tag="ko,en"/>
                                            <Button Content="Korean → Vietnamese" Style="{StaticResource NeutralButton}" Margin="5" Padding="8,3" Click="CommonPair_Click" Tag="ko,vi"/>
                                            <Button Content="Chinese → English" Style="{StaticResource NeutralButton}" Margin="5" Padding="8,3" Click="CommonPair_Click" Tag="ch_sim,en"/>
                                            <Button Content="Chinese → Vietnamese" Style="{StaticResource NeutralButton}" Margin="5" Padding="8,3" Click="CommonPair_Click" Tag="ch_sim,vi"/>
                                        </WrapPanel>
                                    </StackPanel>
                                </Border>

                                <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Language_Note]}"/>
                                <GroupBox Header="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Language_Mapping]}" Margin="0,0,0,15" Padding="10" Foreground="{DynamicResource TextBrush}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                                                Margin="0,15,0,0" TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource TextBrush}">
                                            <Run>ja = Japanese</Run>
                                            <LineBreak/>
                                            <Run>en = English</Run>
                                            <LineBreak/>
                                            <Run>ch_sim = Chinese (Simplified)</Run>
                                            <LineBreak/>
                                            <Run>ko = Korean</Run>
                                            <LineBreak/>
                                            <Run>vi = Vietnamese</Run>
                                            <LineBreak/>
                                            <Run>fr = French</Run>
                                            <LineBreak/>
                                            <Run>ru = Russian</Run>
                                            <LineBreak/>
                                            <Run>de = German</Run>
                                            <LineBreak/>
                                            <Run>es = Spanish</Run>
                                            <LineBreak/>
                                            <Run>it = Italian</Run>
                                            <LineBreak/>
                                            <Run>hi = Hindi</Run>
                                            <LineBreak/>
                                            <Run>da = Danish</Run>
                                        </TextBlock>
                                        <TextBlock Grid.Row="0" Grid.Column="2"
                                            Margin="0,15,0,0" TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource TextBrush}">
                                            <Run>pt = Portuguese</Run>
                                            <LineBreak/>
                                            <Run>ar = Arabic</Run>
                                            <LineBreak/>
                                            <Run>nl = Dutch</Run>
                                            <LineBreak/>
                                            <Run>pl = Polish</Run>
                                            <LineBreak/>
                                            <Run>ro = Romanian</Run>
                                            <LineBreak/>
                                            <Run>fa = Farsi</Run>
                                            <LineBreak/>
                                            <Run>cs = Czech</Run>
                                            <LineBreak/>
                                            <Run>id = Indonesian</Run>
                                            <LineBreak/>
                                            <Run>th = ThaiLand</Run>
                                            <LineBreak/>
                                            <Run>ch_tra = Traditional Chinese</Run>
                                            <LineBreak/>
                                            <Run>tr = Turkish</Run>
                                            <LineBreak/>
                                            <Run>si = Sinhala</Run>
                                        </TextBlock>
                                    </Grid>
                                </GroupBox>
                            </StackPanel>

                            <!-- OCR Settings Panel -->
                            <StackPanel x:Name="OcrPanel" Visibility="Collapsed">
                                <TextBlock TextWrapping="Wrap" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Title]}"/>

                                <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Desc]}"/>

                                <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Desc2]}"/>
                                <Grid Margin="0,0,0,20">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,5,10,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Method]}"/>
                                    <ComboBox Grid.Row="0" Grid.Column="1" x:Name="OcrMethodComboBox" Width="200" HorizontalAlignment="Left" SelectionChanged="OcrMethodComboBox_SelectionChanged"/>
                                    <Button x:Name="setupOCR" Grid.Row="0" Grid.Column="2" Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Setup]}" Style="{StaticResource NeutralButton}" Margin="0,5,0,0" Click="SetupOcrServer_Click"/>
                                </Grid>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,10"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Comparison]}"/>

                                        <!-- Windows OCR -->
                                        <Expander Header="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Windows_Title]}" IsExpanded="True" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}">
                                            <StackPanel Margin="10,5,0,0">
                                                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Windows_Pros]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Windows_Cons]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_Windows_Rec]}"/>
                                            </StackPanel>
                                        </Expander>

                                        <!-- OneOCR -->
                                        <Expander Header="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_OneOCR_Title]}" IsExpanded="True" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}">
                                            <StackPanel Margin="10,5,0,0">
                                                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_OneOCR_Pros]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_OneOCR_Cons]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_OneOCR_Rec]}"/>
                                            </StackPanel>
                                        </Expander>

                                        <!-- RapidOCR -->
                                        <Expander Header="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_RapidOCR_Title]}" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}">
                                            <StackPanel Margin="10,5,0,0">
                                                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_RapidOCR_Pros]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_RapidOCR_Cons]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_RapidOCR_Rec]}"/>
                                            </StackPanel>
                                        </Expander>

                                        <!-- EasyOCR -->
                                        <Expander Header="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_EasyOCR_Title]}" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}">
                                            <StackPanel Margin="10,5,0,0">
                                                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_EasyOCR_Pros]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_EasyOCR_Cons]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_EasyOCR_Rec]}"/>
                                            </StackPanel>
                                        </Expander>

                                        <!-- PaddleOCR -->
                                        <Expander Header="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_PaddleOCR_Title]}" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}">
                                            <StackPanel Margin="10,5,0,0">
                                                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_PaddleOCR_Pros]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_PaddleOCR_Cons]}"/>
                                                <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_OCR_PaddleOCR_Rec]}"/>
                                            </StackPanel>
                                        </Expander>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <!-- Translation Service Panel -->
                            <StackPanel x:Name="TranslationPanel" Visibility="Collapsed">
                                <TextBlock TextWrapping="Wrap" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_Title]}"/>

                                <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_Desc]}"/>

                                <Grid Margin="0,0,0,20">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,5,10,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_Service]}"/>
                                    <ComboBox Grid.Row="0" Grid.Column="1" x:Name="TranslationServiceComboBox" Width="200" HorizontalAlignment="Left" SelectionChanged="TranslationServiceComboBox_SelectionChanged"/>
                                </Grid>

                                <!-- Google Translate API Key -->
                                <StackPanel x:Name="GoogleTranslateApiKeyPanel" Visibility="Collapsed" Margin="0,0,0,10">
                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GoogleKey]}"/>
                                    <PasswordBox x:Name="GoogleTranslateApiKeyPasswordBox" Width="300" HorizontalAlignment="Left" KeyDown="GoogleTranslateApiKeyPasswordBox_PasswordChanged"/>
                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GoogleKeyNote]}"/>
                                </StackPanel>

                                <!-- ChatGPT API Key -->
                                <StackPanel x:Name="ChatGptApiKeyPanel" Visibility="Collapsed" Margin="0,0,0,10">
                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_ChatGPTKey]}"/>
                                    <PasswordBox x:Name="ChatGptApiKeyPasswordBox" Width="300" HorizontalAlignment="Left" KeyDown="ChatGptApiKeyPasswordBox_PasswordChanged"/>

                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,10,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_ChatGPTModel]}"/>
                                    <ComboBox x:Name="ChatGptModelComboBox" Width="300" HorizontalAlignment="Left"/>

                                    <Button Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_ChatGPTKeyBtn]}" Style="{StaticResource NeutralButton}" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="8,3" Click="GetApiKey_Click" Tag="https://platform.openai.com/api-keys"/>

                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_ChatGPTNote]}"/>
                                </StackPanel>

                                <!-- Gemini API Key -->
                                <StackPanel x:Name="GeminiApiKeyPanel" Visibility="Collapsed" Margin="0,0,0,10">
                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GeminiKey]}"/>
                                    <PasswordBox x:Name="GeminiApiKeyPasswordBox" Width="300" HorizontalAlignment="Left" KeyDown="GeminiApiKeyPasswordBox_PasswordChanged"/>

                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,10,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GeminiModel]}"/>
                                    <ComboBox x:Name="GeminiModelComboBox" Width="300" HorizontalAlignment="Left"/>

                                    <Button Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GeminiKeyBtn]}" Style="{StaticResource NeutralButton}" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="8,3" Click="GetApiKey_Click" Tag="https://ai.google.dev/"/>

                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GeminiNote]}"/>
                                </StackPanel>

                                <!-- Groq API Key -->
                                <StackPanel x:Name="GroqApiKeyPanel" Visibility="Collapsed" Margin="0,0,0,10">
                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GroqKey]}"/>
                                    <PasswordBox x:Name="GroqApiKeyPasswordBox" Width="300" HorizontalAlignment="Left" KeyDown="GroqApiKeyPasswordBox_PasswordChanged"/>

                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,10,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GroqModel]}"/>
                                    <ComboBox x:Name="GroqModelComboBox" Width="300" HorizontalAlignment="Left"/>

                                    <Button Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GroqKeyBtn]}" Style="{StaticResource NeutralButton}" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="8,3" Click="GetApiKey_Click" Tag="https://console.groq.com/keys"/>

                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_GroqNote]}"/>
                                </StackPanel>

                                <!-- Mistral API Key -->
                                <StackPanel x:Name="MistralApiKeyPanel" Visibility="Collapsed" Margin="0,0,0,10">
                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_MistralKey]}"/>
                                    <PasswordBox x:Name="MistralApiKeyPasswordBox" Width="300" HorizontalAlignment="Left" KeyDown="MistralApiKeyPasswordBox_PasswordChanged"/>
                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,10,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_MistralModel]}"/>
                                    <ComboBox x:Name="MistralModelComboBox" Width="300" HorizontalAlignment="Left"/>
                                    <Button Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_MistralKeyBtn]}" Style="{StaticResource NeutralButton}" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="8,3" Click="GetApiKey_Click" Tag="https://console.mistral.ai/"/>

                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_MistralNote]}"/>
                                </StackPanel>

                                <!-- Microsoft API Key -->
                                <StackPanel x:Name="MicrosoftApiKeyPanel" Visibility="Collapsed" Margin="0,0,0,10">
                                    <TextBlock Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Lbl_MicrosoftApiKeyQuickstart]}" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"/>
                                    <PasswordBox x:Name="MicrosoftApiKeyPasswordBox" Width="300" HorizontalAlignment="Left" KeyDown="MicrosoftApiKeyPasswordBox_PasswordChanged"/>
                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Msg_MicrosoftLegacyQuickstartNote]}"/>
                                    <CheckBox x:Name="MicrosoftLegacyModeCheckBox" Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Chk_MicrosoftLegacyMode]}" Margin="0,8,0,0" Checked="MicrosoftLegacyModeCheckBox_CheckedChanged" Unchecked="MicrosoftLegacyModeCheckBox_CheckedChanged" />
                                </StackPanel>

                                <!-- Ollama Model -->
                                <StackPanel x:Name="OllamaModelPanel" Visibility="Collapsed" Margin="0,0,0,10">
                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_OllamaModel]}"/>
                                    <TextBox x:Name="OllamaModelTextBox" Width="300" HorizontalAlignment="Left" TextChanged="OllamaModelTextBox_TextChanged"/>

                                    <Button Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_OllamaDownload]}" Style="{StaticResource NeutralButton}" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="8,3" Click="GetApiKey_Click" Tag="https://ollama.com/download"/>

                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_OllamaNote]}"/>
                                </StackPanel>

                                <!-- LM Studio Model -->
                                <StackPanel x:Name="LMStudioModelPanel" Visibility="Collapsed" Margin="0,0,0,10">
                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_LMStudioModel]}"/>
                                    <TextBox x:Name="LMStudioModelTextBox" Width="300" HorizontalAlignment="Left" TextChanged="LMStudioModelTextBox_TextChanged"/>

                                    <Button Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_LMStudioDownload]}" Style="{StaticResource NeutralButton}" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="8,3" Click="GetApiKey_Click" Tag="https://lmstudio.ai/download"/>

                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,5,0,0"
                                               Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_LMStudioNote]}"/>
                                </StackPanel>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,10"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_Recs]}"/>
                                        <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_Speed]}"/>
                                        <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_Beginner]}"/>
                                        <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_Quality]}"/>
                                        <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_Offline]}"/>
                                        <TextBlock TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource TextBrush}"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Translation_Balance]}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <!-- Complete Panel -->
                            <StackPanel x:Name="CompletePanel" Visibility="Collapsed">
                                <TextBlock TextWrapping="Wrap" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Title]}"/>

                                <TextBlock TextWrapping="Wrap" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Review]}"/>

                                <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Success]}"/>

                                <TextBlock TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Summary]}"/>

                                <Border Style="{StaticResource CardStyle}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,10,10"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Languages]}"/>
                                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="LanguagesSummaryText" Text="Japanese → English" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}"/>

                                        <TextBlock Grid.Row="1" Grid.Column="0" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,10,10"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_OCR]}"/>
                                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="OcrMethodSummaryText" Text="Windows OCR" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}"/>

                                        <TextBlock Grid.Row="2" Grid.Column="0" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,10,10"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_TranslationService]}"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TranslationServiceSummaryText" Text="Google Translate" Foreground="{DynamicResource TextBrush}"/>
                                    </Grid>
                                </Border>

                                <TextBlock TextWrapping="Wrap" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"
                                           Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_NextSteps]}"/>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <TextBlock TextWrapping="Wrap" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}">
                                            <TextBlock FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"
                                                       Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Step1Title]}"/>
                                            <LineBreak/>
                                            <TextBlock Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Step1Desc]}"/>
                                        </TextBlock>

                                        <TextBlock TextWrapping="Wrap" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}">
                                            <TextBlock FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"
                                                       Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Step2Title]}"/>
                                            <LineBreak/>
                                            <TextBlock Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Step2Desc]}"/>
                                        </TextBlock>

                                        <TextBlock TextWrapping="Wrap" Margin="0,0,0,10" Foreground="{DynamicResource TextBrush}">
                                            <TextBlock FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"
                                                       Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Step3Title]}"/>
                                            <LineBreak/>
                                            <TextBlock Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Step3Desc]}"/>
                                        </TextBlock>

                                        <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}">
                                            <TextBlock FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"
                                                       Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Step4Title]}"/>
                                            <LineBreak/>
                                            <TextBlock Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_Step4Desc]}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>

                                <Border Background="{DynamicResource Surface3Brush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="10" Padding="15" Margin="0,0,0,20">
                                    <StackPanel>
                                        <TextBlock TextWrapping="Wrap" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,10"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_HelpTitle]}"/>
                                        <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}"
                                                   Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_HelpDesc]}"/>
                                        <Button Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Complete_JoinDiscord]}" Style="{StaticResource AccentButton}" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="10,5" Click="JoinDiscord_Click"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </Grid>

                <!-- ========== FOOTER ========== -->
                <Border Grid.Row="2" Background="{DynamicResource Surface2Brush}" 
                        CornerRadius="0,0,12,12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0"
                        Padding="20,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0" x:Name="DontShowAgainCheckBox" VerticalAlignment="Center"
                            Checked="DontShowAgainCheckBox_CheckedChanged"
                            Unchecked="DontShowAgainCheckBox_CheckedChanged">
                            <TextBlock Foreground="#FF5F57" FontWeight="SemiBold"
                                      Text="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Footer_DontShow]}"/>
                        </CheckBox>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="PrevButton" Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Footer_Previous]}" Style="{StaticResource NeutralButton}" Width="80" Margin="0,0,10,0" Click="PrevButton_Click"/>
                            <Button x:Name="NextButton" Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Footer_Next]}" Style="{StaticResource AccentButton}" Width="80" Click="NextButton_Click"/>
                            <Button x:Name="FinishButton" Content="{Binding Source={x:Static local:LocalizationManager.Instance}, Path=Strings[Quickstart_Footer_Finish]}" Style="{StaticResource AccentButton}" Width="80" Visibility="Collapsed" Click="FinishButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
